  - IP address of a migrated RHEL (or other Linux-based operating
    system) virtual machine is not accessible
    
    You must create a `RHEL premigration` playbook and add it to the
    migration plan.
